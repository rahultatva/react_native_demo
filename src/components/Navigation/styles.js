import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  profileImg: {
    width: 80,
    height: 80,
    borderRadius: 40,
    marginTop: 20,
  },
  wrapper: {
    alignItems: 'center',
  },
});
export default styles;
